---
name: figma-code-connect
description: Use this skill when the user wants to map Figma design components to local code components, mentions "code connect", "映射组件", "连接Figma组件", "map figma to code", "link component", "绑定设计组件", "组件映射", "添加映射", or provides a Figma URL and asks to connect it to a local component. Scans the project's Components directory and uses the Figma MCP plugin to establish Code Connect mappings.
version: 1.1.0
---

# Figma Code Connect — 组件映射 Skill

将 Figma 设计组件映射到本地代码组件（使用 Figma MCP 的 `add_code_connect_map` 工具）。

## 本地组件注册表

项目 `Components/` 目录采用 `ComponentName/index.jsx` 结构。当前已注册组件：

| 组件名 | 路径 | 框架 | Figma 别名（匹配关键词） |
|---|---|---|---|
| Button | `Components/Button/index.jsx` | React | button, btn, cta, Btn/Able, Btn/Unable, Btn/Primary, Btn/Secondary |
| Input | `Components/Input/index.jsx` | React | input, textfield, textinput, text-field, search, InputField |
| Modal | `Components/Modal/index.jsx` | React | modal, dialog, popup, overlay, drawer, sheet, DialogBox |
| Image | `Components/Image/index.jsx` | React | image, img, photo, picture, thumbnail, cover, ImageView |
| NavBar | `Components/NavBar/index.jsx` | React | navbar, nav-bar, navigation, navi, header, NavigationBar, App Navi Bar, TopBar |

新增组件后须同步更新此表。

## 工作流程

### Step 1 — 解析 Figma URL

从用户提供的 Figma URL 中提取 `fileKey` 和 `nodeId`：
- 格式：`https://figma.com/design/:fileKey/:fileName?node-id=:nodeId`
- 将 nodeId 中的 `-` 转换为 `:`（例如 `1-2` → `1:2`）

如果用户未提供 URL，询问 Figma 文件链接。

### Step 2 — 扫描本地组件目录

使用 Glob 工具匹配：`Components/**/index.{jsx,tsx,js,ts,vue,svelte}`

为每个组件提取：
- **组件名**：父目录名（如 `Components/Button/index.jsx` → `Button`）
- **文件路径**：`Components/Button/index.jsx`

优先使用上方注册表中的已知映射，Glob 扫描用于发现新增组件。

### Step 3 — 获取 Figma 节点信息

使用 `get_code_connect_suggestions` 获取 Figma 文件中待映射的组件节点建议。
或使用 `get_metadata` 获取全量节点，过滤 COMPONENT / COMPONENT_SET 类型。

### Step 4 — 匹配组件

将 Figma 节点名称与注册表别名进行匹配（大小写不敏感）：

**匹配规则（优先级从高到低）：**
1. 注册表别名精确匹配
2. 完全匹配（忽略大小写）
3. 包含关系（Figma 名包含组件名，或反之）
4. 无法匹配 → 列出候选项让用户选择

### Step 5 — 调用 add_code_connect_map

对每个匹配的组件调用：

```
fileKey:       <从 URL 提取>
nodeId:        <Figma 组件节点 ID>
source:        <Components/ComponentName/index.jsx>
componentName: <组件名>
label:         React
```

### Step 6 — 汇报结果

```
✅ 映射成功：
  Figma: Btn/Able        →  Components/Button/index.jsx  [React]
  Figma: App Navi Bar    →  Components/NavBar/index.jsx  [React]
  Figma: ImageView       →  Components/Image/index.jsx   [React]

⚠️ 未匹配（需手动处理）：
  Figma: Checkbox  — 未找到对应本地组件
```

## 批量映射

用户可以一次性映射整个 Figma 页面的所有组件：
- 使用 `get_metadata` 获取页面所有节点
- 过滤出 COMPONENT 和 COMPONENT_SET 类型节点
- 对照注册表批量执行匹配和映射

## 注意事项

- 映射是一对一的：一个 Figma 节点对应一个本地组件
- `source` 使用相对路径，便于团队共享
- 映射保存在 Figma 服务端，无需在本地写入额外文件
- 如需查看已有映射，使用 `get_code_connect_map` 工具
- 新增本地组件后，务必同步更新上方**本地组件注册表**
