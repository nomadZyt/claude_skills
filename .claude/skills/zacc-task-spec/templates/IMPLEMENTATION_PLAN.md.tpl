# IMPLEMENTATION_PLAN.md

> 由 **zacc-task-spec** 技能自动生成
> Feature: `{feature-name}`
> 生成时间：{date}
> 当前状态：**{planning|executing|completed|blocked}**

---

## 需求概述

{一段话描述需求背景、目标和验收标准}

## 上游产物依赖

> 记录本规划依赖的上游 Skill 产物状态，供跨 Session 恢复时快速校验。

| 产物 | 路径 | 状态 | 备注 |
|------|------|------|------|
| CLAUDE.md | `CLAUDE.md` | {已加载/未找到} | {技术栈摘要} |
| AI_RULES.md | `.claude/AI_RULES.md` | {已加载/未找到} | {红线条数} |
| Wiki 索引 | `.wiki/index.json` | {已加载/未找到} | {模块数} |
| 术语表 | `.wiki/glossary.md` | {已加载/未找到} | {术语数} |

---

## 1. UI 拓扑图 (UI Topology)

### 组件树

```
{Page}
├── {ContainerComponent}
│   ├── {ChildComponentA}
│   │   └── {LeafComponent}
│   └── {ChildComponentB}
└── {SharedComponent}
```

### 组件分类

| 组件 | 类型 | 来源 | 备注 |
|------|------|------|------|
| {ComponentName} | {通用/业务专用} | {已有/新增} | {已有组件标注 .wiki 节点 ID} |

---

## 2. 状态流动图 (State Flow)

### 数据来源

| 数据 | API 接口 | 请求方式 | 约束 |
|------|---------|---------|------|
| {数据名} | `{method} {path}` | {参数说明} | {缓存/分页等约束} |

### 状态边界

| 状态 | 类型 | 归属 | 说明 |
|------|------|------|------|
| {stateName} | {全局 Store / 局部 State} | {模块/组件} | {生命周期说明} |

### 数据流图

```
路由参数 / 用户交互
       ↓
  页面组件 (Container)
       ↓  dispatch / setState
  API 请求 (via request 封装)
       ↓  response
  Store Action / Local State
       ↓  useSelector / props
  子组件渲染
```

---

## 3. 阶段里程碑 (Milestones)

### M1: {里程碑名称}

> 知识来源：{.wiki 节点 / CLAUDE.md 红线 / 本次新增分析}

- [ ] **T1** (S): {任务描述} — 涉及文件：`{file1}`, `{file2}`

  <details>
  <summary>执行详情（Task 完成后由 Agent 填写）</summary>

  **契约：** {入参/出参签名或 Props 接口定义}
  **关键决策：** {为什么选择这种实现方式，排除了哪些替代方案}
  **变更文件：** {实际变更的文件列表，标注 新增/修改}
  **关联 commit：** {commit hash}
  **依赖：** {前置 Task ID 列表，如 T0(类型定义)}
  **被依赖：** {下游 Task ID 列表，如 T3(集成)}
  **回滚：** {回滚方式 + 影响评估，如 "git revert xxx，无下游状态副作用可安全独立回滚" 或 "需先回滚 T3 再回滚本 Task"}
  </details>

- [ ] **T2** (M): {任务描述} — 涉及文件：`{file1}`, `{file2}`, `{file3}`, `{file4}`

  <details>
  <summary>执行详情（Task 完成后由 Agent 填写）</summary>

  **契约：** {入参/出参签名或 Props 接口定义}
  **关键决策：** {核心设计决策及理由}
  **变更文件：** {实际变更的文件列表，标注 新增/修改}
  **关联 commit：** {commit hash}
  **依赖：** {前置 Task ID}
  **被依赖：** {下游 Task ID}
  **回滚：** {回滚方式 + 影响评估}
  </details>

### M2: {里程碑名称}

> 知识来源：{...}

- [ ] **T3** (M): {任务描述} — 涉及文件：`{...}`

  <details>
  <summary>执行详情（Task 完成后由 Agent 填写）</summary>

  **契约：** {...}
  **关键决策：** {...}
  **变更文件：** {...}
  **关联 commit：** {...}
  **依赖：** {...}
  **被依赖：** {...}
  **回滚：** {...}
  </details>

- [ ] **T4** (S): {任务描述} — 涉及文件：`{...}`

  <details>
  <summary>执行详情（Task 完成后由 Agent 填写）</summary>

  **契约：** {...}
  **关键决策：** {...}
  **变更文件：** {...}
  **关联 commit：** {...}
  **依赖：** {...}
  **被依赖：** {...}
  **回滚：** {...}
  </details>

### M3: {里程碑名称}

> 知识来源：{...}

- [ ] **T5** (M): {任务描述} — 涉及文件：`{...}`

  <details>
  <summary>执行详情（Task 完成后由 Agent 填写）</summary>

  **契约：** {...}
  **关键决策：** {...}
  **变更文件：** {...}
  **关联 commit：** {...}
  **依赖：** {...}
  **被依赖：** {...}
  **回滚：** {...}
  </details>

### M4: {里程碑名称}（如需）

> 知识来源：{...}

- [ ] **T6** (S): {任务描述} — 涉及文件：`{...}`

  <details>
  <summary>执行详情（Task 完成后由 Agent 填写）</summary>

  **契约：** {...}
  **关键决策：** {...}
  **变更文件：** {...}
  **关联 commit：** {...}
  **依赖：** {...}
  **被依赖：** {...}
  **回滚：** {...}
  </details>

---

## 4. 历史包袱约束

> 从 `.wiki/nodes/` 的 Legacy Constraints 章节提取，与本需求相关的约束。

| 来源节点 | 约束描述 | 影响范围 | 应对策略 |
|---------|---------|---------|---------|
| {node_id} | {约束内容} | {受影响的 Task} | {规避/兼容方案} |

---

## 5. 执行日志

> 每个 Task 完成后由 Agent 自动追加记录。

| Task | 完成时间 | 结果 | 备注 |
|------|---------|------|------|
| {T1} | {datetime} | {通过/回退} | {如有回退，记录原因} |

---

## 6. 阻碍与变更记录

| 日期 | 类型 | 描述 | 处理方式 |
|------|------|------|---------|
| {date} | {阻碍/变更/升级} | {描述} | {人工介入/重新规划/已解决} |
