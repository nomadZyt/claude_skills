---
project: "{project_name}"
last_updated: "{ISO-8601}"
---

# {project_name} — 术语表

> 此文件由 **zacc-init-wiki-fronted** 执行 **SOP-1（初始化）** 后自动生成，从模块名、类名、核心方法名中提取领域术语。
> 帮助 AI 理解项目的领域语言，提高代码生成的准确性。

## 领域术语

| 术语 | 定义 | 别名/缩写 | 对应代码实体 | 所属模块 |
|------|------|-----------|------------|---------|
| {term} | {definition} | {alias_or_abbr} | `{code_entity}` | [[{module_id}]] {module_title} |

## 缩写对照

| 缩写 | 全称 | 上下文 |
|------|------|--------|
| {abbr} | {full_name} | {在哪里使用} |

## 检索映射

> 这里的术语、缩写、历史名应同步回写到对应节点 frontmatter 的 `aliases`，避免术语只存在于 glossary 中而不参与检索。

| 检索词 | 映射节点 | 命中原因 |
|--------|---------|---------|
| {lookup_term} | [[{module_id}]] {module_title} | alias / glossary / abbreviation |

## 命名模式

> 记录项目中的命名约定，帮助 AI 生成风格一致的代码。

| 模式 | 示例 | 适用范围 |
|------|------|---------|
| {pattern} | `{example}` | {scope} |
