# PRD 文档模板

> 供 zacc-prd-by-html 步骤 5 使用，定义生成的 PRD 文档结构、每章节内容和格式。

---

# {产品名称} 产品需求文档 (PRD)

**版本**: v1.0
**文档日期**: {YYYY-MM-DD}
**原型来源**: {文件路径或 URL}
**生成方式**: zacc-prd-by-html 反向工程

---

## 1. 产品概述

### 1.1 产品定位

| 项目 | 内容 |
|------|------|
| 产品名称 | {从 `<title>` 或 logo 提取} |
| Slogan | {从 hero 区域或 meta 提取} |
| 产品类型 | {工具/平台/内容/社交/电商...} |
| 核心价值 | {从功能组合归纳} |

### 1.2 目标用户

| 用户角色 | 特征 | 核心诉求 |
|----------|------|----------|
| {角色1} | {从页面文案推断} | {从功能用途归纳} |

> ⚠️ 标注"待确认"的条目为原型中未明确体现，需产品经理补充。

---

## 2. 用户流程

### 2.1 核心用户旅程

```
[入口页面] → [关键操作] → [结果页面]
     ↓
[分支路径] → [备选操作] → [备选结果]
```

> 使用 ASCII 流程图，从 `<a href>` 和按钮操作提取。

### 2.2 页面导航关系

| 源页面 | 导航方式 | 目标页面 |
|--------|----------|----------|
| {页面A} | {点击/提交/跳转} | {页面B} |

### 2.3 关键交互节点

| 节点 | 触发方式 | 交互类型 | 结果 |
|------|----------|----------|------|
| {按钮/表单} | {点击/提交} | {确认/弹窗/跳转} | {操作结果} |

---

## 3. 信息架构

### 3.1 页面模块层级

```
{页面名称}
├── {一级模块}
│   ├── {二级模块}
│   └── {二级模块}
├── {一级模块}
└── {一级模块}
```

### 3.2 导航体系

| 导航类型 | 位置 | 包含入口 |
|----------|------|----------|
| {顶部导航/侧边栏/底部Tab} | {页面位置} | {入口列表} |

---

## 4. 功能需求

### 4.1 功能模块概览

| 模块 | 功能点数量 | 优先级 | 说明 |
|------|-----------|--------|------|
| {模块名} | {N} | {P0/P1/P2} | {一句话说明} |

### 4.2 详细功能需求

#### {模块名称}

**FD-{NNN} {功能名称}**

| 项目 | 内容 |
|------|------|
| 用户故事 | As a {角色}, I want to {操作}, so that {目的} |
| 触发条件 | {何时触发} |
| 前置条件 | {需要满足什么} |
| 操作流程 | 1. {步骤1} 2. {步骤2} ... |
| 业务规则 | {约束和规则} |
| 输入 | {字段列表及校验规则} |
| 输出 | {操作结果} |
| 异常处理 | {异常场景和处理方式} |
| 原型对应 | {HTML元素定位} |

---

## 5. 各页面详细需求

### 5.{N} {页面名称}

**页面路径**: {URL 或文件路径}
**页面入口**: {从哪些页面可到达}

#### 页面元素清单

| 元素 | 类型 | 说明 | 交互行为 |
|------|------|------|----------|
| {元素名} | {按钮/表单/列表...} | {用途} | {点击/提交/展开...} |

#### 数据展示

| 数据项 | 展示方式 | 数据来源 |
|--------|----------|----------|
| {字段名} | {文本/列表/表格...} | {API/本地} |

---

## 6. 交互设计规范

### 6.1 色彩系统

| 类型 | 色值 | 用途 |
|------|------|------|
| 主色 | {hex} | 主按钮、强调 |
| 辅助色 | {hex} | 次要元素 |
| 成功色 | {hex} | 成功提示 |
| 警告色 | {hex} | 警告提示 |
| 错误色 | {hex} | 错误提示 |

### 6.2 字体规范

| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| H1 | {size} | {weight} | 页面标题 |
| H2 | {size} | {weight} | 模块标题 |
| 正文 | {size} | {weight} | 正文内容 |

### 6.3 组件规范

| 组件 | 规格说明 |
|------|----------|
| 按钮 | {圆角/尺寸/颜色变体} |
| 输入框 | {圆角/高度/边框} |
| 卡片 | {圆角/阴影/内边距} |

### 6.4 动画与过渡

| 场景 | 动画类型 | 持续时间 |
|------|----------|----------|
| {页面切换/弹窗/展开} | {fade/slide/scale} | {ms} |

---

## 7. 非功能性需求

| 类型 | 需求 | 原型依据 |
|------|------|----------|
| 性能 | {从动画/交互推断} | {CSS/JS 依据} |
| 兼容性 | {从 meta/CSS 推断} | {viewport/媒体查询} |
| 无障碍 | {从 ARIA/语义标签推断} | {role/aria-* 属性} |

---

## 8. 数据需求

### 8.1 数据实体

| 实体 | 字段 | 来源 |
|------|------|------|
| {实体名} | {字段列表} | {表单/API/列表推断} |

### 8.2 数据流转

```
{页面A} → [提交] → {API} → {存储} → {页面B} [展示]
```

---

## 9. 运营策略

| 运营点 | 配置方式 | 原型依据 |
|--------|----------|----------|
| {Banner/弹窗/推荐位} | {配置化/硬编码} | {元素位置和内容} |

---

## 10. 待确认项汇总

| 编号 | 章节 | 待确认内容 | 原因 |
|------|------|-----------|------|
| TBC-001 | {章节} | {具体内容} | 原型未体现 |
| TBC-002 | {章节} | {具体内容} | 信息不完整 |

---

## 11. 附录

### A. 原型文件清单

| 文件 | 页面数 | 说明 |
|------|--------|------|
| {文件路径} | {N} | {页面用途} |

### B. 设计建议

> 以下为分析过程中发现的设计问题或优化建议，非需求。

| 编号 | 建议 | 原因 |
|------|------|------|
| S-001 | {建议内容} | {发现的问题} |
